extends layouts/main-layout.pug
//- Anypage that extends another template file
//- needs block content to render contents

//- Take the below block and put it in
//- template that is being extended (here, main-layout)
//-  call createPostForm mixin
block content

    +createPostForm(userLoggedIn)

